services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      PORT: "3001"
      TURSO_DATABASE_URL: file:./data/smart-invoice.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped